# Copy this file to .env and fill in real values.
# .env itself is git-ignored (see root .gitignore).

DATABASE_URL=postgresql://parkkeys_user:parkkeys_pass@localhost:5432/parkkeys_db
JWT_SECRET_KEY=change-this-to-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
